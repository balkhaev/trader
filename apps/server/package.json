{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js",
    "telegram:auth": "bun run src/scripts/telegram-auth.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.2",
    "technicalindicators": "^3.1.0",
    "@trader/auth": "workspace:*",
    "@trader/db": "workspace:*",
    "@trader/env": "workspace:*",
    "@types/archiver": "^7.0.0",
    "archiver": "^7.0.1",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.8.2",
    "openai": "^6.15.0",
    "playwright": "^1.57.0",
    "telegram": "^2.26.22",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:",
    "tsdown": "^0.16.5",
    "@trader/config": "workspace:*"
  }
}
