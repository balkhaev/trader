name: Altcoin OI history round 30

on:
  pull_request:
    paths:
      - "research/altcoin-round30-history/**"
      - ".github/workflows/altcoin-oi-history-round30.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 240
    env:
      PYTHONUNBUFFERED: "1"
      PANDAS_COPY_ON_WRITE: "1"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "numpy>=2.0,<3" "pandas>=2.2,<3" "requests>=2.32,<3" "tabulate>=0.9,<1"

      - name: Restore verified archive cache
        uses: actions/cache@v4
        with:
          path: .cache/binance-altcoin-round30
          key: altcoin-round30-usdm-oi-history-2024-2025h1-v1

      - name: Compile runner
        run: |
          python -m py_compile research/altcoin-round28-confluence/run.py
          python -m py_compile research/altcoin-round30-history/run_history.py

      - name: Run fixed historical robustness check
        run: |
          rm -rf artifacts/altcoin-round30-history
          mkdir -p artifacts/altcoin-round30-history
          python research/altcoin-round30-history/run_history.py \
            --output artifacts/altcoin-round30-history \
            --cache .cache/binance-altcoin-round30 \
            --workers 24
          cp research/altcoin-round30-history/run_history.py artifacts/altcoin-round30-history/

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: altcoin-round30-history-results
          path: artifacts/altcoin-round30-history
          if-no-files-found: warn
          retention-days: 7
          compression-level: 6
