name: Funding adaptive round 51

on:
  pull_request:
    paths:
      - "research/altcoin-round50-funding-full/run_adaptive.py"
      - ".github/workflows/funding-adaptive-round51.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 240
    env:
      PYTHONUNBUFFERED: "1"
      PANDAS_COPY_ON_WRITE: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "numpy>=2.0,<3" "pandas>=2.2,<3" "requests>=2.32,<3" "tabulate>=0.9,<1"
      - name: Restore verified funding cache
        uses: actions/cache@v4
        with:
          path: .cache/binance-funding-full-round50
          key: funding-full-round50-usdm-2024-2026h1-v1
      - name: Compile and run causal adaptive selector
        run: |
          python -m py_compile research/altcoin-round27-premium/*.py research/altcoin-round41-funding-carry/run.py research/altcoin-round50-funding-full/run.py research/altcoin-round50-funding-full/run_adaptive.py
          rm -rf artifacts/funding-adaptive-round51
          mkdir -p artifacts/funding-adaptive-round51
          PYTHONPATH=research/altcoin-round27-premium python research/altcoin-round50-funding-full/run_adaptive.py \
            --output artifacts/funding-adaptive-round51 \
            --cache .cache/binance-funding-full-round50 \
            --workers 32
          cp research/altcoin-round50-funding-full/run_adaptive.py artifacts/funding-adaptive-round51/
      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: funding-adaptive-round51-results
          path: artifacts/funding-adaptive-round51
          if-no-files-found: warn
          retention-days: 7
          compression-level: 6
